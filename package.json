{
  "name": "marka-kopru",
  "version": "1.0.0",
  "private": true,
  "description": "Statik koprü — tek package.json (kök). Render: Root boş, Build: npm install, Start: npm start",
  "dependencies": {
    "serve": "^14.2.4"
  },
  "scripts": {
    "start": "serve -s . -l $PORT",
    "start:local": "serve -s . -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
