{
  "name": "marka-kopru-serve",
  "version": "1.0.0",
  "private": true,
  "description": "Kökte index.html; serve bir üst klasörü (..) açar — Render bu src/package.json ister.",
  "scripts": {
    "start": "serve .. -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
